//Write a program to find whether the number is prime or not.

#include <stdio.h>
int main()
{
    int a,i;

    printf("Enter the Number: ");
    scanf("%d", &a);

    for(i=2;i<=a;++i){
    if(a%i==0);}

    printf("%d", i);

    return 0;

}